<%= form_for(@tweet) do |f| %>
	<% if @tweet.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@note.errors.count, "error") %> prohibited this tweet from being saved:</h2>

			<ul>
				<% @note.errors.full_messages.each do |message| %>
					<li><%= message %></li>
				<% end %>
			</ul>
		</div>
		<% end %>

		<div class="field">
			<%= f.label :handle %><br />
			<%= f.text_area :handle, :placeholder => "Your handle" %>
			<% temp_handle = :handle %>
				<% if temp_handle[0] == "@" %>
					<% write_attribute(:handle, temp_handle.from[1]) %> 
					<!-- not sure why this doesn't work... 
					I'll ask to see the solution to this 
					extra credit later -->
				<% end %>
		</div>
		<div class="field">
			<%= f.label :content %><br />
			<%= f.text_area :content, :placeholder => "Your tweet content" %>
		</div>
		<div class="actions">
			<%= f.submit %>
		</div>
		<% end %>